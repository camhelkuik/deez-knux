{{page-title "Signup"}}

<LoginSignupForm
    @formTitle={{this.title}}
    @formAction={{this.signup}}
    @updateAction={{this.update}}
    @valueType={{this.valueType}}
    @error={{this.error}}
/>